package com.shortbridge;

import org.junit.jupiter.api.Test;

class ShortBridgeApplicationTests {

  @Test
  void contextLoadsPlaceholder() {
    // Full @SpringBootTest 는 Testcontainers + RabbitMQ + Postgres 가 필요하므로
    // 빌드 가능성 검증용 placeholder 만 둔다.
  }
}
